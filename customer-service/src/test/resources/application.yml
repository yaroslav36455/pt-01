testcontainers:
  postgres:
    version: 15.10

spring:
  datasource:
    driver-class-name:
    url:
    username: test
    password: test
  r2dbc:
    url:
    username: test
    password: test
  liquibase:
    change-log: "classpath:/db/changelog/db.changelog-test.yaml"
    enabled: true
    driver-class-name:
    url:
    user: test
    password: test
  cloud:
    loadbalancer:
      enabled: false

eureka:
  client:
    enabled: false

service:
  document:
    url: resource-service